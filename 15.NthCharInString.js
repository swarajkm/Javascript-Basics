var myStr = "hello world";

//suppose we want the last char d, then we do it as:
/* basically what we did was within the brackets we gave the length-1 position number to get 'd' */
var lastChar = myStr[myStr.length - 1];
console.log(lastChar);

//suppose we want the 3rd last char
var NthlastChar = myStr[myStr.length - 3];
console.log(NthlastChar);
